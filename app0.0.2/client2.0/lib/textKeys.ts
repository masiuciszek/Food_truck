interface Key {
  [key: string]: string
}

const textKeys: Key = {
  ContactUsFormTitle: `Let's get in touch`,
  ContactUsFormText: `we will always answer and would love to hear from you 💗`,
}

export default textKeys
